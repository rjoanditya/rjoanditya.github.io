# rjoanditya.github.io

Portfolio website of Rizky Joanditya, refactored to Next.js (App Router) and configured for static export to GitHub Pages.

## Development

```bash
npm install
npm run dev
```

## Build for GitHub Pages

```bash
npm run build
```

Build output is generated in `out/`.
